#nacos配置
server:
  port: 4800

spring:
  application:
    name: cloud-nacos-final-order
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 #nacos作为配置中心
        file-extension: yaml #指定yaml格式配置
        group: majf-dev-test
        namespace: 85bc68b5-8c6c-4ef4-a139-0a0c08a57763